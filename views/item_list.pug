extends layout 

block content 
  h1= title 
  ul 
    each item in item_list 
      li
        a(href=item.url) #{item.name}

    else 
      li There are no items available.

  
  a(href="/catalog/item/new") Add new item  
    
